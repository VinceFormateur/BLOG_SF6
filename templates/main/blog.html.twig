{% extends 'base.html.twig' %}

{% block title %}Blog{% endblock %}

{% block body %}
	<div class="container">

		<h1>Mes articles</h1>

		{{ dump(posts) }}

{# 		{% for post in posts %}

			<p>{{ post.title }}</p>
			<p>{{ post.content }}</p>

		 	<p>{{ post.createdAt|date("d/m/Y") }}</p>

		{% endfor %}
 #}
	</div>
{% endblock %}